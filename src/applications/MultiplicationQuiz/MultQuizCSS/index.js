import css from "./MultQuiz.module.css";
import { DancingScriptR } from "../../../fonts";

const Container = `${css.Container} ${DancingScriptR}`;
const Outputs = css.Outputs;
const Output = css.Output;
const KeyboardButtons = css.KeyboardButtons;
const KeyboardButton = `${css.KeyboardButton} ${DancingScriptR}`;

export { Container, Outputs, Output, KeyboardButtons, KeyboardButton };
